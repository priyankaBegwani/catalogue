# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# Backend Configuration
PORT=3001
VITE_BACKEND_URL=http://localhost:3001

# Image Storage Configuration
# Options: 'cdn', 'supabase', or 'local'
# - 'cdn': Use Wasabi/Cloudflare CDN (production, requires CDN setup)
# - 'supabase': Use Supabase Storage (fallback, requires bucket setup)
# - 'local': Use local filesystem (development/testing, no external setup needed)
STORAGE_TYPE=local

# CDN Storage (Wasabi + Cloudflare) - Only needed if STORAGE_TYPE=cdn
WASABI_ENDPOINT=https://s3.wasabisys.com
WASABI_REGION=us-east-1
WASABI_ACCESS_KEY_ID=your_wasabi_access_key
WASABI_SECRET_ACCESS_KEY=your_wasabi_secret_key
WASABI_BUCKET_NAME=indie-craft-designs
CLOUDFLARE_CDN_URL=https://your-cdn-url.com

# Notes:
# - STORAGE_TYPE=local: Images stored in backend/uploads/ (no setup needed)
# - STORAGE_TYPE=supabase: Create 'design-images' bucket in Supabase
# - STORAGE_TYPE=cdn: Configure Wasabi and Cloudflare credentials above

#for indiecraft.online
# Wasabi Storage Configuration
WASABI_ACCESS_KEY_ID=SXGABHCWTITXQCCQKUEA
WASABI_SECRET_ACCESS_KEY=CVXsFJEUARtaOhrupMTcHC5AwYHDfr6cy8HVjrcH
WASABI_BUCKET_NAME=ic-designs
WASABI_REGION=ap-southeast-1
WASABI_ENDPOINT=https://s3.ap-southeast-1.wasabisys.com
